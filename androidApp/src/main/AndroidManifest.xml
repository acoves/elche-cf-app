<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- FASE 6: necesario para AppWebView (tienda/entradas/membership) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ElcheApplication"
        android:allowBackup="true"
        android:label="Elche CF"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <!-- FASE 2: sustituir el tema del sistema por ElcheTheme y añadir icono de marca (mipmap/ic_launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
